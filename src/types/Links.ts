export interface InternalLinks {
  base: string;
  favicon: string;
  home: string;
  projects: string;
  resume: string;
}

export interface ExternalLinks {
  mailTo: string;
  github: string;
  githubRepo: string;
  astro: string;
}
