import type { InternalLinks } from '@models/Links';

const internal: InternalLinks = {
  base: 'https://dev.pustalorc.com',
  favicon: '/favicon.png',
  home: '/',
  projects: '/projects',
  resume: '/resume'
};

export default internal;
